# Copy to .env and adjust. Never commit .env.

DATABASE_URL=ecto://postgres:postgres@localhost/backend_dev
SECRET_KEY_BASE=                    # mix phx.gen.secret
FRONTEND_URL=http://localhost:5173

# Email (prod only — dev uses the local Swoosh mailbox at /dev/mailbox)
MAIL_FROM=noreply@tickets.dev
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
