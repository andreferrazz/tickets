# Copy to .env.local and adjust for your environment.

# Leave empty to use the built-in SvelteKit mock API (no backend needed).
# Point to the real Phoenix backend to use live data.
VITE_API_URL=http://localhost:4000/api/v1
